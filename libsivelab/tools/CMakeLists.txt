include_directories(${CMAKE_SOURCE_DIR})
link_directories(${CMAKE_CURRENT_BUILD_DIR})

IF (UNIX)
#add_executable (autoGenTestGrids
#	       autoGenTestGrids.cpp)
#target_link_libraries (autoGenTestGrids sive-quicutil) 
#target_link_libraries (autoGenTestGrids sive-util) 
ENDIF(UNIX)
